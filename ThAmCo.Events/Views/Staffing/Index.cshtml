@model IEnumerable<ThAmCo.Events.Data.Staffing>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Event)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Event.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Staff.StaffEmail)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.StaffID, id1 = item.EventID }) |
                @Html.ActionLink("Details", "Details", new {  id=item.StaffID  }) |
                @Html.ActionLink("Delete", "Delete", new {  id=item.StaffID, eventId = item.EventID  })
            </td>
        </tr>
}
    </tbody>
</table>
